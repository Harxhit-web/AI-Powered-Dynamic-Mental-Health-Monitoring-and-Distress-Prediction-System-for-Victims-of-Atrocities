import { useMemo, useState } from "react";
import { BellRing, CalendarClock, Check, CheckCircle2, Clock3, PhoneCall, Pill, ShieldCheck, Sparkles } from "lucide-react";

const activities = [
  { id: "breathing", title: "Grounding breathing", detail: "Follow the 4-minute guided exercise from your care plan.", time: "4 min", tone: "indigo" },
  { id: "reflection", title: "Mood reflection", detail: "Share how your body and mind feel right now.", time: "3 min", tone: "violet" },
  { id: "support", title: "Support check", detail: "Review one person or place you can contact if you need support.", time: "2 min", tone: "emerald" },
];

const medications = [
  { id: "morning", name: "Sertraline", instruction: "50 mg · Take with food", time: "8:00 AM", status: "Morning" },
  { id: "evening", name: "Hydroxyzine", instruction: "As prescribed for anxiety or sleep", time: "9:00 PM", status: "Evening" },
];

const toneStyle = { indigo: "bg-indigo-50 text-indigo-700", violet: "bg-violet-50 text-violet-700", emerald: "bg-emerald-50 text-emerald-700" };

export default function Checkin() {
  const [completed, setCompleted] = useState([]);
  const [medicationsTaken, setMedicationsTaken] = useState([]);
  const [canAnswer, setCanAnswer] = useState("yes");
  const [callRequested, setCallRequested] = useState(false);
  const [notice, setNotice] = useState("");
  const completedCount = completed.length;
  const activityProgress = useMemo(() => Math.round((completedCount / activities.length) * 100), [completedCount]);
  const toggle = (id, setter) => setter((current) => current.includes(id) ? current.filter((value) => value !== id) : [...current, id]);

  return <div className="mx-auto max-w-6xl space-y-6 pb-8">
    <section className="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><p className="mb-1 text-sm font-semibold text-indigo-600">Daily check-in · Monday, 8 September</p><h2 className="text-2xl font-bold tracking-tight text-slate-900 sm:text-3xl">Your plan for today</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Move through these steps at your own pace. Your care team can view only the information you choose to share.</p></div><div className="rounded-xl border border-indigo-100 bg-indigo-50 px-4 py-3"><p className="text-xs font-medium text-indigo-600">Today’s progress</p><p className="mt-1 text-lg font-bold text-indigo-900">{completedCount} of {activities.length} activities complete</p></div></section>
    {notice && <div className="flex items-center justify-between rounded-xl border border-indigo-100 bg-indigo-50 px-4 py-3 text-sm text-indigo-800"><span>{notice}</span><button onClick={() => setNotice("")} className="font-bold" aria-label="Dismiss">×</button></div>}

    <section className="grid gap-6 xl:grid-cols-[1.55fr_1fr]">
      <div className="space-y-6">
        <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2"><span className="flex h-7 w-7 items-center justify-center rounded-full bg-indigo-600 text-sm font-bold text-white">1</span><div><p className="text-sm font-medium text-slate-500">Start with your wellbeing</p><h3 className="text-xl font-bold text-slate-900">Today’s activities</h3></div></div></div><span className="rounded-full bg-slate-100 px-3 py-1.5 text-xs font-semibold text-slate-600">{activityProgress}% complete</span></div><div className="mt-5 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-indigo-600 transition-all" style={{ width: `${activityProgress}%` }} /></div><div className="mt-4 divide-y divide-slate-100">{activities.map((activity, index) => { const done = completed.includes(activity.id); return <button key={activity.id} onClick={() => toggle(activity.id, setCompleted)} className="flex w-full items-center gap-3 py-4 text-left first:pt-0 last:pb-0"><span className={`flex h-7 w-7 shrink-0 items-center justify-center rounded-full border-2 transition ${done ? "border-emerald-500 bg-emerald-500 text-white" : "border-slate-300 text-slate-400"}`}>{done ? <Check size={15} strokeWidth={3} /> : index + 1}</span><div className="min-w-0 flex-1"><p className={`text-sm font-bold ${done ? "text-slate-400 line-through" : "text-slate-800"}`}>{activity.title}</p><p className="mt-1 text-sm text-slate-500">{activity.detail}</p></div><span className={`shrink-0 rounded-lg px-2 py-1 text-xs font-bold ${toneStyle[activity.tone]}`}>{activity.time}</span></button>; })}</div></article>

        <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center gap-2"><span className="flex h-7 w-7 items-center justify-center rounded-full bg-indigo-600 text-sm font-bold text-white">2</span><div><p className="text-sm font-medium text-slate-500">Follow your prescription</p><h3 className="text-xl font-bold text-slate-900">Medication schedule</h3></div></div><div className="mt-5 space-y-3">{medications.map((medication) => { const taken = medicationsTaken.includes(medication.id); return <div key={medication.id} className="flex flex-col gap-3 rounded-xl border border-slate-200 p-4 sm:flex-row sm:items-center"><div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-indigo-50 text-indigo-600"><Pill size={20} /></div><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><p className="text-sm font-bold text-slate-800">{medication.name}</p><span className="rounded-md bg-slate-100 px-2 py-0.5 text-[11px] font-semibold text-slate-600">{medication.status}</span></div><p className="mt-1 text-sm text-slate-500">{medication.instruction}</p></div><div className="flex items-center justify-between gap-3 sm:block sm:text-right"><p className="flex items-center gap-1 text-sm font-bold text-slate-700"><Clock3 size={15} className="text-indigo-500" /> {medication.time}</p><button onClick={() => toggle(medication.id, setMedicationsTaken)} className={`mt-0 rounded-lg px-3 py-1.5 text-xs font-bold transition sm:mt-2 ${taken ? "bg-emerald-50 text-emerald-700" : "bg-indigo-600 text-white hover:bg-indigo-700"}`}>{taken ? "Taken" : "Mark taken"}</button></div></div>; })}</div><p className="mt-4 rounded-lg bg-amber-50 px-3 py-2 text-xs leading-5 text-amber-800"><b>Important:</b> Take medication only as prescribed by your clinician. Contact your care team or pharmacist if you have questions about a dose or side effects.</p></article>
      </div>

      <div className="space-y-6">
        <article className="overflow-hidden rounded-2xl border border-indigo-200 bg-white shadow-sm"><div className="bg-indigo-600 p-5 text-white"><div className="flex items-center gap-2"><BellRing size={20} /><p className="text-sm font-semibold text-indigo-100">Step 3 · Scheduled support</p></div><h3 className="mt-2 text-xl font-bold">IVRS wellbeing call</h3><p className="mt-1 text-sm leading-5 text-indigo-100">A short automated call for your daily check-in.</p></div><div className="p-5"><div className="flex items-center gap-3 rounded-xl bg-indigo-50 p-4"><CalendarClock size={22} className="text-indigo-600" /><div><p className="text-sm font-bold text-slate-800">Today at 6:30 PM</p><p className="mt-1 text-xs text-slate-500">Your registered phone number</p></div></div><fieldset className="mt-5"><legend className="text-sm font-bold text-slate-800">Will you be able to answer?</legend><div className="mt-3 grid gap-2"><button onClick={() => setCanAnswer("yes")} className={`rounded-xl border px-3 py-3 text-left text-sm font-semibold transition ${canAnswer === "yes" ? "border-indigo-500 bg-indigo-50 text-indigo-700" : "border-slate-200 text-slate-600 hover:border-indigo-200"}`}>Yes, call me at 6:30 PM</button><button onClick={() => setCanAnswer("later")} className={`rounded-xl border px-3 py-3 text-left text-sm font-semibold transition ${canAnswer === "later" ? "border-indigo-500 bg-indigo-50 text-indigo-700" : "border-slate-200 text-slate-600 hover:border-indigo-200"}`}>Not then — call me later</button><button onClick={() => setCanAnswer("skip")} className={`rounded-xl border px-3 py-3 text-left text-sm font-semibold transition ${canAnswer === "skip" ? "border-indigo-500 bg-indigo-50 text-indigo-700" : "border-slate-200 text-slate-600 hover:border-indigo-200"}`}>I cannot take a call today</button></div></fieldset><button onClick={() => setNotice(canAnswer === "yes" ? "Your IVRS call is confirmed for 6:30 PM." : canAnswer === "later" ? "A request to reschedule your IVRS call has been saved." : "Today’s IVRS call has been skipped.")} className="mt-4 w-full rounded-xl bg-indigo-600 py-2.5 text-sm font-bold text-white hover:bg-indigo-700">Save call preference</button></div></article>

        <article className="rounded-2xl border border-emerald-200 bg-emerald-50 p-5 shadow-sm"><div className="flex items-center gap-2 text-emerald-800"><PhoneCall size={20} /><p className="text-sm font-bold">Want support now?</p></div><p className="mt-2 text-sm leading-6 text-emerald-900">You can request a call from your care team when you would like to talk.</p><button onClick={() => { setCallRequested(true); setNotice("Your request for a care-team call has been sent."); }} className={`mt-4 flex w-full items-center justify-center gap-2 rounded-xl py-2.5 text-sm font-bold transition ${callRequested ? "bg-emerald-200 text-emerald-800" : "bg-emerald-700 text-white hover:bg-emerald-800"}`}>{callRequested ? <><CheckCircle2 size={17} /> Call requested</> : <><PhoneCall size={17} /> Request a call</>}</button></article>

        <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-2"><Sparkles size={19} className="text-amber-500" /><h3 className="font-bold text-slate-800">A reminder for today</h3></div><p className="mt-3 text-sm leading-6 text-slate-600">A check-in is not a test. Share only what feels safe and manageable for you today.</p><div className="mt-4 flex items-center gap-2 text-xs font-semibold text-slate-500"><ShieldCheck size={15} className="text-emerald-600" /> Your responses are handled privately.</div></article>
      </div>
    </section>
  </div>;
}
