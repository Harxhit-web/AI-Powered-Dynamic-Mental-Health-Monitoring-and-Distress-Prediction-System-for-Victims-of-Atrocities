# URL-encode special characters in the password: @ becomes %40, # becomes %23.
DATABASE_URI=postgresql://postgres:your_url_encoded_password@localhost:5432/mental_health
# Required only to create the first admin through POST /admins.
ADMIN_SETUP_KEY=replace_with_a_long_random_secret
